<%- include("partials/header") %>

<% blogs.forEach(function(blog){ %>
   <h3><%= blog.title %></h3>
<img src="<%= blog.image %>">
<p>
	<%= blog.introduction %>
</p>
<p>
	<%= blog.body %>
</p>

<a class='btn btn-info' href="/blog/<%= blog._id %>">Read More</a>
  
<%	});%>
<%- include("partials/footer") %>